How to add a program to docker

look at requirements.txt for the format for libraries (such as with Flask)
with database technologies being able to be added such as the MongoDB example.

docker-compose up --build